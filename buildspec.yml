version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
      - mvn test 
  post_build:
    commands:
      - echo Build completed on `date`
      - mvn package
artifacts:
  files:
    - /opt/codedeploy-agent/deployment-root/347f569f-e515-4ee3-ad8c-69ed8f0745ba/d-3QLWHURES/deployment-archive/my-app-1.0-SNAPSHOT.jar
    - appspec.yml
  discard-paths: no
